<resources>
    <!-- Base application theme. -->
    <style name="Theme.ChatKitAndroid" parent="Theme.Material3.DynamicColors.DayNight">

        <!-- Status bar color. -->
        <item name="android:statusBarColor">@android:color/system_accent2_0</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:navigationBarColor">@android:color/system_accent2_0</item>

    </style>

    <style name="Theme.ChatKitAndroid.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
</resources>